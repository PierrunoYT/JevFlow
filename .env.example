# Only used when explicitly passing --jev. Never include keys in market data.
TYPESAFE_API_KEY=

# Binance Spot Testnet HMAC credentials ONLY. Never use production keys here.
BINANCE_TESTNET_API_KEY=
BINANCE_TESTNET_API_SECRET=
